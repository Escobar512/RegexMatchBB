<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.regexbb">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegexBB"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".EdtiPerfil_EM"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Ofertas" />
        <activity
            android:name=".register"
            android:label="Offer" />
        <activity
            android:name=".offerList"
            android:label="Lista de ofertas" />
        <activity
            android:name=".chatList"
            android:label="Lista de chat" />
        <activity
            android:name=".create_vac"
            android:label="Lista de ofertas" />
        <activity
            android:name=".create_perfil"
            android:label="crear/editar perfil" />

    </application>

</manifest>