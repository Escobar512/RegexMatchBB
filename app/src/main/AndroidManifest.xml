<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.regexbb">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegexBB"
        android:usesCleartextTraffic="true">

        <activity

            android:name=".MessageList"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".verLooker"
            android:label="Looker"
             />

        <activity
            android:name=".verOffer"
            android:label="Offer"
            />

        <service android:name=".sockets.SocketIOService" />

<!--        <activity-->
<!--            android:name=".MainActivity"-->
<!--            android:label="Ofertas"-->
<!--            />-->
        <activity
            android:name=".register"
            android:label="Offer"
            />
        <activity
            android:name=".offerList"
            android:label="Lista de ofertas"
        />

    </application>

</manifest>